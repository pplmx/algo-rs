services:
    algo-rs:
        build: .
        image: algo-rs
        ports:
            - 8000:8000

networks:
    algo-rs-net:
        name: algo-rs-net
        ipam:
            config:
                -   subnet: 172.16.238.0/24
